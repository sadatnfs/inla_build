language: c
sudo: required
cache: packages
dist: trusty

os:
  - linux

before_install:
  - sudo apt-get update
  - sudo apt-get install -y libxml2-dev aptitude wget libgsl0-dev r-mathlib

addons:
  apt:
    packages:
      - libxml2-dev 
      - aptitude 
      - wget 
      - libgsl0-dev 
      - r-mathlib


# services:
#   - docker

# before_install:
#   - docker pull rocker/geospatial
#   - docker run /bin/bash rocker/geospatial -c 'R -e "print('tH')"'


# r_packages:
#   - sp
#   - iterators
#   - foreach
#   - parallel
#   - doParallel


before_install:  
  - chmod +x bin/01_clone_and_download_deps.sh
  - chmod +x bin/02_build_inla.sh

jobs:
  include:
    - stage: All builds in one stage
      r: release
      install: true
      script: 
        - ./bin/01_clone_and_download_deps.sh
        - ./bin/02_build_inla.sh

    # - stage: Build all the binaries
    #   r: release
    #   script: 
    #     - ./bin/02_build_inla.sh


notifications:
  email:
    on_success: never
    on_failure: never