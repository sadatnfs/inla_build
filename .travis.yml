language: r

sudo: false

cache: packages

os:
  - linux

apt_packages:
  - libmetis-dev
  - libmetis5
  - aptitude
  - libgsl
  - libgsl-dev 
  - libgsl0-dev 
  - libgsl2


# services:
#   - docker

# before_install:
#   - docker pull rocker/geospatial
#   - docker run /bin/bash rocker/geospatial -c 'R -e "print('tH')"'


r_packages:
  - sp
  - iterators
  - foreach
  - parallel
  - doParallel


jobs:
  include:
    - stage: Install libraries
      r: 3.5
      install: true
      script:
        - wget https://gist.githubusercontent.com/sadatnfs/e8a0ca5fe821fb4d95b2b944face8fb3/raw/735e035d63b74a6cfea317f75a8c13d961595f69/Makefile
        - make lib-install

notifications:
  email:
    on_success: never
    on_failure: never